<% if @user.errors.any? %>
  <div class="alert alert-error">
    There are <%= pluralize(@user.errors.count, "error") %> on this page that need amending before submission can be successful.
  </div>
<% end %>