<% provide(:title, "#{@production.title}") %>

<%= @production.title %>

<%= link_to 'Edit Production', edit_production_path(@production) %>
<%= link_to 'Delete Production', production_path(@production), method: :delete %>